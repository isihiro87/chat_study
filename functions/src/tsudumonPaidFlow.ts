/**
 * つづもんの**有料受付を止めるための単一スイッチ**。
 *
 * ## 経緯（2026-08-06）
 * 2026-08-04 15:01、一問一答の登録者 3,289人へ先行公開を告知した。2日後の実績:
 *
 *   告知 3,289 → 友だち追加 64（1.9%）→ 教材を開いた 21 → 体験 14 → **課金 0**
 *
 * 追加した64人のうち44人（69%）はあいさつを受け取ったあと1歩も動かなかった。
 * この数字を受けて、**有料の単独プロダクトとしては畳み、既存の公式LINE
 * （一問一答・3,000人・無料）の充実に寄せる**判断をした。
 *
 * 止める時点で **Stripe のサブスクは1件も存在しない**（課金・トライアル中・
 * 支払い失敗すべてゼロ）。返金も解約も発生しないので、入口を塞ぐだけで足りる。
 *
 * ## このフラグが false のときに起きること
 * - Stripe Checkout を作らない（`tsudumonCreateCheckout` が 410 を返す）
 * - 体験終了まわりの文面から「自分で続ける（1,280円）」を出さない
 * - あいさつ・保護者向けの案内から購入導線を出さない
 *
 * ## 止めないもの（意図的）
 * - つづもんBot と教材そのもの。既存の64人はそのまま使える
 *   （体験ライセンスは失効させず期限を延長した。`scripts/_extend-tsudumon-licenses.ts`）
 * - Stripe の**アカウント**。プレミアム（ぐっとスクール）と相乗りしているので、
 *   止めるのは商品・価格の単位で行う。アカウントには触らない
 * - 一問一答（`lineWebhook`）の挙動・文言。3,000人のサービスは無風で維持する
 *
 * ## 再開するときは
 * ここを true に戻すだけでは足りない。LP（`pdf-workbook/lp/index.html`）の
 * 「受付停止」表示、`/account/` `/parents/` の申込導線、Stripe 側の商品の
 * 有効化も戻すこと。`docs/operations/tsudumon-paid-shutdown.md` を参照。
 */
export const TSUDUMON_PAID_FLOW_ENABLED = false;
