# 使用状況スナップショット（追記式・再取得不要）

公式LINE の「最近の使用状況」と「当日登録者のテスト範囲設定状況」を、Firestore 読み取りコスト規律（CLAUDE.md）を守って定点取得した記録。取得スクリプト: `scripts/_usage-scope-snapshot.ts`（count() 集計 + limit クエリのみ、全件 .get() なし）。

**新しい取得をしたら、このファイルの先頭付近にブロックを1つ追記する**（古いものは消さない）。同じ期間は再取得しない。

---

## 2026-07-12 取得 — 登録者サマリ＋ファネル（週次定点）

取得スクリプト: `scripts/report-user-stats.ts` / `scripts/report-funnel-stats.ts`（管理者2件除外）。比較ベースは 07-06（登録2,335）。

### 規模・活性（登録2,822）
- 登録総数 **2,822**（07-06比 +487／6日 ≈ **81人/日で増勢維持**）。LINE連携99.8%。
- 1問でも回答 **2,552（90.4%）** / 直近7日回答UU **1,716（60.8%）** / 直近30日 **2,373（84.1%）**。
- status: active **61.7%** / at-risk 9.9% / dormant 9.5% / **churned 18.8%（07-06 14.7%→蓄積進行、コホート加齢）**。
- 累計回答 **78,341問**（6日で+16,451 ≈ **2,740問/日**）。正答率 **60.8%**（横ばい・適正）。

### 最大のポジティブ: 範囲設定が 68.6% に到達
- **testScope 設定済 1,935/2,822 = 68.6%**（06-15 は≈25-32%・新規初日11%が「最大の弱点」だった）。
- 経路の **93.5% が `line_inline`（トーク内Quick Reply逐次選択フロー、2026-06-16導入）**＝LIFF/webを介さない動線が決定打。旧 web `/scope` 経由の funnel（liff_scope_save 7d=4件）はもはや実態を表さない。
- KPI目標（最低30%〜理想70%）のほぼ理想値に到達。「①範囲設定率が唯一の構造的赤信号」は**解消**。

### 学習エンゲージメント（7d）
- 回答は answers 8,000件上限に到達（実数はそれ以上）。経路別: extra 77%・daily 19%・weak_review 正答率81%。
- 「追加で解く」タップ 7d **16,362回**（回答後カード起点 15,058）＝深掘り学習が主エンジン。
- 「まだ習ってない」指摘の集中: 大衆文化の成熟24・戦後の文化21・気体の性質21・人類の出現19 等（配信順序見直しの入力）。
- おためし1問（友だち追加直後）: 回答363・正答率76.6%。

### Win-back（定点）
- 送信UU 1,442 → 復帰検知 **39.7%**（うち winback_cta 569/572＝ワンタップCTAがほぼ全て）→ **status→active 回復 UU 430（送信比29.8%）**。回復元は at-risk 342 / dormant 70 / churned 22。
- 07-06時点で穴だった `status_transition→active` の計測が蓄積開始＝復帰の質が取れるようになった。

### 弱点（既知・継続）
- ニックネーム入力 2.2%（61人）＝パーソナライズ死蔵のまま。
- churned 530人（18.8%）と絶対数が拡大。1問以下離脱層の初期活性化リークは未解消。
- プレミアム/課金系イベントは全機能無料化(2026-06)後の残骸で動きなし（premium有効0・意図通り）。

---

## 2026-07-06 取得 — 登録者サマリ＋申込ファネル（良い面/悪い面/気になる面）

取得スクリプト: `scripts/report-user-stats.ts`（users/answers/stats の count集計）/ `scripts/report-funnel-stats.ts`（premiumFunnelEvents 集計）。管理者2件除外。比較ベースラインは 06-15（登録784・週1学習UU514・正答率53.3%）。

### 規模・活性（登録2,335）
- 登録総数 **2,335**（06-15比 784→2,335 ＝ **約3倍**、07-01比 1,929→2,335）。LINE連携99.8%。
- 1問でも回答したUU **2,101（90.0%）** / 直近7日回答UU **1,445（61.9%、514→約2.8倍）** / 直近30日 **1,999（85.6%）**。
- status: active **64.7%** / at-risk 9.6% / dormant 11.0% / churned **14.7%**。
- 累計回答 **61,890問**（1回答者あたり平均29.5問）。正答率 **60.9%**（53.3%→+7.6pt）。

### 良い面
- 登録が3週で3倍、かつ「登録だけ層」が薄い（回答率90%・30日85.6%）。
- 正答率が 53.3→60.9% に改善＝学習として機能。
- **Win-back のワンタップCTAが復帰の主役**：restart検知421UUのうち **420UUが winback_cta 経由**（keyword入力は2のみ）。
- 回答後カードの追撃が強力：7日で「追加で解く」14,730タップ、主経路 `extra`（正答率64%）。

### 悪い面 / 弱点
- **ニックネーム入力率2.3%（53人）**＝パーソナライズがほぼ死蔵。
- 離脱層 合計約35%（churned343 + dormant257 + at-risk223）。母数増で絶対数が無視できない規模に。
- **範囲設定の浸透 約1割**（`liff_scope_save` UU=227）。open→save は76%と高く、問題は「設定画面まで来ないこと」。KPI目標25%に未達。
- プレミアム/課金は実質ゼロ（premium有効0・申込送信2UU・全員 trial_expired）＝**2026-06 の全機能無料化・課金導線無効化の意図通り**。ファネル数値は残骸で実害なし。

### 気になる面（計測の穴）
- **「オンボーディング完了0.0%」は表示バグ**：実データは `state=complete` 2,236件（95.8%）。型定義 `completed`／実書込 `complete` の不一致で分母判定がズレるだけ＝数値は正常、レポート表示のみ壊れ。
- **`status_transition→active` が0件記録**＝Win-backの「実際にactive回復した人数」が未取得（cron蓄積が浅い）。復帰の質が測れない。
- 申込ファネルの順序逆転（info_view 26UU < apply_view 71UU）。課金廃止済みで実害小。
- 「まだ習ってない」指摘が理科・化学に集中（身のまわりの物質13・化学変化12・気体9…）＝配信の単元順序/時期の見直し入力。
- 直近7日 answers 集計が8,000件上限で頭打ち（実数はこれ以上）。

### 評価まとめ
- **獲得とエンゲージメントは順調**（登録3倍・回答率90%・正答率改善・Win-back CTAが機能）。
- **次の一手候補**: ①離脱層35%の内訳深掘り、②範囲設定の動線強化（到達率10%→）、③オンボ完了0%表示バグ修正、④復帰“回復”計測の穴埋め。

---

## 2026-07-06 追記 — 離脱層35%の深掘り＋バグ修正2件（①③④対応）

取得スクリプト: `scripts/_churn-breakdown.ts`（users を1回スキャン、追加 answers 読みなし＝read規律遵守。user doc の status/stats.totalAnswered/lastAnsweredAt/blocked/testScope/winbackHistory/onboardingStartedAt で判定）。

### ① 離脱層の内訳（結論: 離脱はファネル破綻でなく "コホート加齢" が主因）
- **コホート別 active率**（登録時期フィールド coverage 99.6%）:
  - 直近7日（576人）**active 92.9%** / churned 0%
  - 8–30日前（1,356人）active 58.0% / churned 12.5%
  - 31日以上前（399人）active 46.9% / churned 43.1%
  - → 直近獲得コホートはほぼ全員活性。古いコホートの想定内減衰。獲得エンジンは健全。
- **離脱3層の"質"は別物**:
  - **at-risk(223) / dormant(257) = 高価値の休眠**: 平均 **33.7問 / 21.2問**、範囲設定 **74% / 81%**、Win-back受信 96% / 88%。1問以下離脱は 4.9% / 6.6%。→ 回復可能な資産、レバーは Win-back（既にほぼ全員に到達）。
  - **churned(343) = 初期活性化リーク本体**: 平均 **6.7問**、**1問以下離脱 23.3%**、範囲設定 **36%**、ブロック **30.9%**。
- **範囲設定は"活性の分水嶺"**: 範囲設定率 churned 36% ≪ at-risk/dormant 74–81%。設定した人は圧倒的に定着 → 範囲設定動線（②）は**リテンション施策**と裏付け。
- ブロック率は churn 深度と相関: at-risk 4.9% → dormant 17.5% → churned 30.9%。

### ③ オンボ完了0%表示バグ 修正済み（検証済み）
- 原因: 書き込み実体 `onboardingState:"complete"` に対し、集計 `report-user-stats.ts` と型 `userDocTypes.ts` が `"completed"` を参照する不一致。
- 修正: `report-user-stats.ts`（"complete" を数える・旧値互換）+ `userDocTypes.ts`（型 union を "complete" に）。→ 表示 **0.0% → 95.8%（2,241人）** に正常化。

### ④ 復帰計測の穴（status_transition→active が永久に0）修正済み ※要デプロイ
- 原因: 非active→active の復帰を ①`onAnswerCreated`（回答による自然復帰）②`handleRestartIntent`（CTA/キーワード）の2箇所で処理するが、どちらも `status_transition` を記録せず、夜間 cron `recalculateUserStatuses` はその後に走るため下り遷移しか観測できない。
- 修正: 両箇所で `status_transition {from, to:'active', source}` を記録（source= answer / winback_cta / keyword）。`restart_intent_detected`（意図）と併存で「確定復帰」を分離計測。型チェック EXIT=0。
- **デプロイ後の復帰から蓄積**。デプロイ: `FUNCTIONS_DISCOVERY_TIMEOUT=600 firebase deploy --only functions:lineWebhook,functions:onAnswerCreated`（2026-07-06 時点 未デプロイ）。

### ② 範囲設定 動線強化（案・未着手）
入口（リッチメニュー/オンボ完了flex/回答後ナッジ/設定サポートflex）は既存だが全体約1割しか設定せず、open→save は76%＝**ボトルネックは"開始させること"**。案（推奨順）:
1. **"おまかせ範囲"のワンタップ既定化**（学年全範囲で承認1タップ→後で絞れる。設定ハードルを構造除去）。
2. **弱点タッチの計測分解**（nudge種別ごとの scope_start 発火率、デプロイ不要）。
3. **文言を便益直結**（「範囲設定」→「定期テストの範囲だけ出す」）。
4. **範囲未設定の at-risk/dormant の Win-back に範囲設定CTA同梱**。

---

## 2026-07-07 追記 — ②範囲設定の計測分解 ＋【重要訂正】範囲設定率は約1割ではなく67.6%

取得スクリプト: `scripts/_scope-funnel-breakdown.ts`（users + premiumFunnelEvents を各1回スキャン）。標準レポート `report-user-stats.ts` にも「出題範囲設定済（testScope）」行を常設した。

### 🔴 重要訂正: 範囲設定率は「約1割」ではなく **67.6%（1,598/2,364）**
- 07-06 に「範囲設定 約1割（liff_scope_save UU=227）」と書いたのは**誤り**。`liff_scope_save` は **web /scope ページ経由だけ**を計測しており、主力の**トーク内 inline フロー（`testScope.lastSource='line_inline'`、funnel 未記録）を丸ごと取りこぼしていた**。
- 実際の保存経路内訳（設定済 1,598 のうち）: **line_inline 91.8%（1,467）** / (無印) 7.6% / **page 0.6%（10）**。web ページはほぼ使われておらず、2026-06-16 追加の inline Quick Reply フロー（多くはオンボ内で完結）が主力。
- → **「範囲設定は弱点」という前提が崩れた**。inline フローは機能している。今後の実態値は `report-user-stats.ts` の testScope 行を見る（funnel の scope_save は web のみと明記済み）。

### 分解できた軸（現状データ）
- **status 別 設定率（リテンション相関は残る）**: active 70.7% / at-risk 75.8% / dormant 79.6% / **churned 41.3%**。churned だけ低い＝早期離脱者は設定前に去る（churn 分析の平均6.7問・1問以下離脱23%と整合）。
- **コホート別**: 直近7日 69.9% / 8-30日前 74.6% / **31日以上前 41.5%**。古層が低いのは inline フロー（06-16 追加）以前の登録で好フローを受けていないため。新規は約70-75%で健全。
- **回答後ナッジ（scopeSetupNudge、唯一いま個別計測できる経路）**: 受信842人中 **転換43.8%（369）/ 受けても未設定 56.2%（473）**。うち**3回全部受けて未設定が248人**＝ナッジ疲れ/CTA弱の可能性。ただし設定済1,598の大半（1,229）は**ナッジ受信前（=オンボ内 inline）で設定済**＝ナッジは"オンボでスキップした人への二次回収"の位置づけ。

### まだ計測できない盲点（計装すれば取れる）
- **4タッチポイント別（リッチメニュー/オンボ完了flex/回答後ナッジ/設定サポートflex）の scope_start 発火・転換**は、`scope_start` postback が**無タグ・funnel 未記録**のため不可能。
- inline フローの**開始→保存 転換**も open 相当イベントが無く取れない（web は open→save 76.2%）。
- → 計装案（要デプロイ）: 各 `scope_start` postback に `&src=<touchpoint>` を付与＋`handleScopeStartPostback` で `scope_start{src}` を funnel 記録。これで次月から touchpoint 別に分解可能。scope_start 発火点は lineWebhook.ts の 5 箇所（494 / 2324 / 2987 / 3790 / 3869 付近）。

### 結論・次の一手
- 範囲設定は**健全（68%）**。当初の「動線強化が最優先」は撤回。残る弱点は① churned/古層（過去分は回収困難、新規は既に高い）と② 回答後ナッジの二次回収率43.8%（248人が3回無視）。
- 投資対効果的には **回答後ナッジの文言/回数見直し**（低コスト）＞ 4タッチポイント計装（要デプロイ・戦略的価値は低下）。計装するなら「次月から正しく測る」目的で。

---

## 2026-07-07 追記 — 回答後 範囲設定ナッジの文言/回数 見直し（実装済み・要デプロイ）

対象: `functions/src/onAnswerCreated.ts`（cadence + A/B 適用）/ `functions/src/lineWebhook.ts`（`buildScopeSetupNudgeFlexMessage`）/ `functions/src/userDocTypes.ts`（型）。型チェック EXIT=0・functions 全213テスト通過。

### 変更内容
1. **文言バグ削除**: ナッジ本文末尾の「LINEアプリを完全に終了して開き直して…」注記を撤去。`scope_start` はトーク内 Quick Reply で完結しアプリ再起動不要＝この注記は LIFF 時代の名残で不要な摩擦だった。
2. **コピー刷新**: 便益をテスト対策直結に（「定期テストの範囲を教えてくれたら、その単元だけ毎日1問…総復習がラクに」）＋「タップで選ぶだけ・あとから変更OK・30秒」で心理ハードルを低減。
3. **回数/タイミング**: `SCOPE_SETUP_NUDGE_MILESTONES` を **1/5/10問目・最大3回 → 3/8問目・最大2回** に変更。理由: 「3回全部受けて未設定=248人」でナッジ疲れが判明。1問目はオンボ直後で無駄打ちになりやすく 3問目へ後ろ倒し。
4. **A/B バリアント**: `pickScopeNudgeVariant(uid)`（uid ハッシュで固定、Math.random 不使用）で A（便益直球）/ B（ムダ削減・手間の低さ）の2文言を出し分け、`users.scopeSetupNudgeVariant` に記録。`_scope-funnel-breakdown.ts` にバリアント別設定率の集計を追加（デプロイ後から蓄積、現状ベースライン転換 43.6%）。
5. **提案B-1（痛点トリガー）は実装せず**: 「まだ習ってない」タップ→`handleNotLearnedApplyPostback` が既に testScope を保存（未設定者は実質の範囲設定代行、funnel: tap 380UU→applied 220UU=68.8%）。痛点トリガーは既存機能でカバー済みのため重複回避。

### デプロイ
`FUNCTIONS_DISCOVERY_TIMEOUT=600 firebase deploy --only functions:lineWebhook,functions:onAnswerCreated`（前回④の復帰計測修正と同じ対象。まとめて反映可能。2026-07-07 時点 未デプロイ）。

---

## 2026-07-01 取得 — サービス全体スナップショット＋7日詳細＋問題品質監査

取得スクリプト: `scripts/_service-snapshot.mjs`（count()集計中心）/ `scripts/_seven-day-analysis.mjs`（直近7日 answers を最小フィールドで全スキャン）/ `scripts/_audit-question-defects.mjs`（問題マスタ全問の不備監査）。tsx が Windows ホストで動かない（node_modules が Linux 版 esbuild）ため、firebase-admin を使う `.mjs` を node 直実行。総まとめは `docs/operations/line-kpi-dashboard.md`。

### 規模・成長（登録1,929）
- 登録総数 **1,929**（06-26比 1,552→1,929）。新規 直近7日 **475** / 直近30日 **1,579 ＝ 全登録の82%が直近30日に集中**（獲得エンジンが強い一方、定着はこれから）。
- ブロック率 **11.1%**（215/1,929、低位安定）。到達可能（非ブロック）1,714。

### 活性
- **週1学習UU（北極星）= 1,260**（登録比65.3% / **非ブロック比73.5%**）。06-15比 +145%（514→1,260）。
- **月間アクティブ（30日）= 1,691 ＝ 登録の87.7%**。
- status: active **67.1%** / at-risk 10.2% / dormant 10.0% / **churned 12.5%（蓄積進行）**。

### 学習量・正答率
- 累計回答 **49,654問**（累計正答率 **60.7%**、30,119正答）。
- **直近7日 24,173問 ＝ 累計のほぼ半分**（直近の学習量が急増）。週1アクティブ1人あたり ≈ **19問/週**。
- 7日正答率（管理者除外・24,127問）**62.7%**。教科別: 地理70.4 > 理科64.1 > 英語61.8 > 歴史60.1。**数学は直近7日で回答1問のみ＝1教科死蔵**。

### 問題品質監査（問題マスタ 5,563問・全問）
- **構造不備0**（4択でない/空/重複選択肢/correctChoiceId範囲外＝すべてゼロ）。
- 累計20回以上・正答率がランダム(25%)未満の22問を目視照合 → **正解キー誤りは0件**（＝良問の難問。誤選択肢がよく効いている）。
- 唯一「設問が曖昧」だった `q-history-social-movements-q5`（婦人参政権・正答率10%、誤答の平塚らいてうも正解と読めた）を **本日修正＋デプロイ済み**（設問限定＋選択肢を樋口一葉へ差替、正解=市川房枝は不変）。

### 評価まとめ
- **強み**: 爆発的成長（2週で登録2.5倍）＋異例の高活性（週1 65%/月間88%）＋正答率60〜63%の適正難易度＋コンテンツ品質堅牢。
- **要警戒**: ①新規コホート（直近30日82%）のD7/D30定着が未検証、②数学の死蔵、③配信枠が7月後半に上限接近（`_push-forecast.ts` 投影）。

---

## 2026-06-15 取得 — KPIダッシュボード ベースライン（計測の穴3点を含む）

取得スクリプト: `scripts/report-user-stats.ts` / `scripts/report-funnel-stats.ts` / `scripts/report-kpi-gaps.ts`（count集計+limit/単一selectスキャンのみ）。総まとめは `docs/operations/line-kpi-dashboard.md`。

### 全体（登録784・管理者2除外）
- 登録784 / LINE連携99.4% / オンボ完了(state=complete)93.8%
- 学年98.3%（中2=469中心）/ 教科96.6%（history756）/ 配信時刻94.1%（20時250・18時212）
- status: active 72.8% / dormant 17.0% / at-risk 9.9%（06-08の一括満了ウェーブが正常化）
- 回答: 1問以上82.8% / 直近7日514(65.6%=**北極星**) / 直近30日649 / 正答率53.3% / answers累計5,766

### 計測の穴3点（`report-kpi-gaps.ts`）
- **ブロック率 15.1%**（119/786）。無回答層28.7% vs 回答経験あり12.3%。**ブロックの46.2%が登録48h以内**＝初日体験がブロックを左右。
- **継続率(survival proxy)**: D1=70.6%(534/756) / D7=54.9%(228/415) / D30=算出不可（登録<30日が大半・サービス若年、7月以降に計上）。起点は `onboardingStartedAt`（`createdAt` フィールドは存在しない）。
- **追加学習率(2問以上/日)=19.1%**（605/3,160セッション）。1人あたり30日回答8.8問。
- **誤答後の解説閲覧率は計測不能＝構造上ほぼ100%**（解説は回答ごとに reply 自動同梱: `lineWebhook.ts` handleAnswer `combinedText`）。学習深度は追加学習率で代替。

### ファネル（`report-funnel-stats.ts`・管理者51除外）
- 範囲設定: liff_scope_open UU253 → **liff_scope_save UU197（≈25%）**
- premium: richmenu_info_tap UU59 → premium_info到達 UU22 → apply_view UU70 → **apply_submit UU2**。premiumApplications 1件。premium有効0（有料未提供）。

### 課題（優先順）
1. **範囲設定率≈25%・新規初日11%**が唯一の構造的赤信号 → 登録直後の動線強化。
2. **無回答層ブロック28.7%＋ブロック46%が48h以内** → 初日「まず1問」誘導で獲得・継続・ブロック同時改善。
3. 穴: 配信開封率（OA Manager）/ D30継続（7月以降）。

---

## 2026-06-09 取得 — 前日(2026-06-08)の日次サマリー

取得スクリプト: `scripts/_yesterday-report.ts`（count集計 + limitクエリのみ、約1,100 read）。取得時刻 06-09 10:29 JST。

### 配信（問題が届いた数）※push 1通ごとのログが無いため `lastQuestionDeliveredAt` ベースの推計
- 昨日が最終配信日のユーザー: **238**／今日すでに再配信済み(朝6・7時枠): 76 → **昨日の配信ユーザー数 ≈ 238〜314（実勢 ~290）**
- 配信時刻分布（サンプル237）: 20時=107 / 18時=82 / 16時=28 / 6時=11 / 7時=8 / 19時=1 → **夜（18〜20時）に集中**

### 解いた数・解いたユーザー数
- 昨日の回答数: **241**／解いたユニークユーザー: **151**
- 正答率: 126/241 = **52.3%**
- 1人あたり平均: **1.60問**／2問以上解いた人: **28人（18.5%）**
- 多出題トピック: 琉球とアイヌ・桃山文化・秀吉の政策と朝鮮出兵・信長秀吉の天下統一(各13) ほか（中2「近世」帯）

### 範囲設定状況
- 配信を受けた人の範囲設定済: **65/237 = 27.4%**
- active サンプル(344)の範囲設定済: **95 = 27.6%**（プロフィール有329では28.9%）
- 参考: **当日新規登録者の範囲設定は11%**（06-08ブロック）→ 既存ユーザーは約28%、新規が極端に低い

### 配信→回答の転換（概算）
- 解いたユニーク 151 / 配信 ≈238〜314 → **約48〜63%（実勢 ~52%）**

### 数値から見える課題（06-08）
1. **配信した問題の約半分しか解かれていない（転換~52%）**。届いてもタップされない層が半数。最大の改善余地。回答リマインド/配信文言/時刻最適化が打ち手候補。
2. **範囲設定が全体28%・新規はわずか11%**。7割が範囲未設定＝学年全体から出題されテスト直結度が低い。**新規の初日設定が最弱点**（設定完了サマリーのボタンが効いていない）。範囲設定の動線強化が最有効。
3. **追加学習（2問以上）は18.5%どまり**。「1問解く」「苦手を復習」「じっくり学ぶ」を全員無料開放した直後なので、ここを伸ばす導線（回答後の next-step 提示）が次の山。
4. **配信が夜帯（20時107・18時82）に偏重**。夜の通知過密に注意。朝枠は少数。生活リズムに合えば問題ないが、満足度・ブロック率の観点で時刻分散も一案。
5. 正答率52%は難易度適正レンジ（維持でOK）。
6. （注）配信数は厳密なログが無く推計。正確な日次配信通数が要るなら、`dailyQuiz` に日次カウンタ（`deliveryStats` を日別doc化 or 専用ログ）を入れると今後ログ取得不要で取れる。

---

## 2026-06-08（JST 22:44 取得）

取得コスト: 約 516 read（status/総数/回答数は count 集計でほぼ無料、内訳は answers 直近500件サンプル + 当日登録者9件）。

### ユーザー status 分布（count 集計・管理者含む）— 総数 435
| status | 人数 | 割合 |
|---|---|---|
| active | 434 | 99.8% |
| at-risk | 0 | 0% |
| dormant | 0 | 0% |
| churned | 0 | 0% |
| status無 | 1 | 0.2% |

### 回答ボリューム（count 集計）
- 直近24h の回答数: **235**
- 直近7日 の回答数: **2,053**

### 直近回答サンプル（最新500件 = 直近約36時間 / 最古 2026-06-07 10:27 JST）
- サンプル内ユニーク回答者: 24h=**149人** / 直近36h=182人
- サンプル正答率（直近36h）: 272/500 = **54.4%**
- 多出題トピック上位: 信長・秀吉の天下統一(27) / ヨーロッパの変革(27) / 江戸幕府の成立(23) / 鎖国下の対外関係(22) / 琉球とアイヌ(21) / 桃山文化(20) / 鎖国への道(19) / 秀吉の政策と朝鮮出兵(17)

### 今日（2026-06-08）の登録者 + テスト範囲設定状況（管理者除く）
- 今日の登録者数: **9**
- うちオンボ完了（配信時刻まで設定）: **9（100%）**
- うちテスト範囲 設定済み: **1（11.1%）**
- うちブロック: 1
- 学年分布: 中3=4 / 中2=4 / 中1=1
- 教科分布: history=9

### 所見（2026-06-08）
1. **DAU ≈ 149人 / 235回答（24h）**、回答/人 ≈ 1.58。毎日1問＋「1問解く」等の追加利用が回っている。母数435に対し DAU 約34%。
2. **status=active 99.8% は「トライアル一括付与の満了ウェーブ」による一過性**（recalc バグではない・下の検証参照）。
3. **当日登録者のオンボ完了は100%だが、テスト範囲設定はわずか11%**。範囲未設定だと学年全体から出題され、テスト直結度が下がる。設定完了サマリーの「出題範囲を設定する」ボタン＋回答後 scope nudge（最大3回）があるが効いていない → 範囲設定の動線強化が次の打ち手（任意・優先度中）。
4. 正答率 54.4% は難易度が概ね適正（易しすぎ・難しすぎでない）レンジ。

### status=active 過大の検証（2026-06-08・決着）
スクリプト: `scripts/_status-verify.ts` / `_status-verify2.ts` / `_status-verify3.ts`（各 active を limit400 サンプル）。

- active サンプル400件中 **plan=premium が 322**、うち **premiumUntil 有効は 91 のみ**（＝231 は「plan=premium だが期限切れ」のゴーストプレミアム。`expireTrialUsers` 廃止で plan が free に戻らないため。ただし getUserPlan/recalc は premiumUntil>now で判定するので**実害なし・全機能無料の今は無視可**）。
- active かつ「最終回答4日以上前」= 96件。内訳: premium有効30（正しく active）/ **premium失効65 / free1**。
- その「詰まり66件」の premiumUntil 失効日を見ると **65件が全て本日(06-08)失効**＝今朝02:00の recalc 実行後に切れただけ。過去日失効での放置は実質0（free1のみ）。
- **結論: `recalculateUserStatuses` は正常。** computeStatusFromLastAnswer も `premiumUntil>now` を正しくチェックしている。06-01 の一括トライアル付与（7日）が **06-08〜数日で一斉満了**しているための一過性。**明日06-09 02:00 以降、満了＋無回答ユーザーが順次 at-risk→dormant→churned に正常化**する。→ **修正不要**。
- 副作用として、今後数日 **Win-back（at-risk=Day3 ほか）が一気に発火**する見込み（満了トライアル層の無回答者がまとめて節目入りするため）。Win-back は節目1回・上限ありで、名前/価格文言も撤去済みなので暴発はしないが、push 通数の一時増は監視推奨（`monthlyDeliveryReport`）。

> 注: 「直近7日のユニーク回答者」は本サンプル（最新500件＝約36h）では取得していない。7日ユニークが必要なら limit を増やすか answers の7日 where + 集計が要る（読み取りコスト増に注意）。
